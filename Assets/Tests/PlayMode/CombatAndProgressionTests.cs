// CombatAndProgressionTests.cs — SAFE PLACEHOLDER
// Purpose: unblock compilation when the Unity Test Framework is not installed.
// This placeholder contains no [UnityTest] or NUnit references.
// You can delete this file later or replace it with the real tests when ready.

namespace KongSurvivors.Tests
{
    // Intentionally empty.
    // When you want automated PlayMode tests:
    // 1) Window → Package Manager → Install "Test Framework"
    // 2) Replace this file with the real tests package I provided earlier
    //    (or ask me to resend a fresh copy).
    internal static class DisabledPlayModeTests { }
}
